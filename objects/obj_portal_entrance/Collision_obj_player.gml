/// @description Insert description here
// You can write your code in this editor


global.camera.target_x -= x_offset;
global.camera.current_x -= x_offset;
global.camera.target_y -= y_offset;
global.camera.current_y -= y_offset;

other.x -= x_offset;
other.y -= y_offset;